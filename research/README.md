# Standalone research

Optional self-contained studies that sit outside the `runs/<id>/` workflow can live here.

A fresh clone has no studies in this folder. Create a subdirectory when you need one; outputs stay local and are gitignored.
